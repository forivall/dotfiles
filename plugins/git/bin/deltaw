#!/usr/bin/env zsh

local deltaOpts=()
if (( ${+commands[dark-mode]} )) && [[ $(dark-mode status) == off ]]
then
	deltaOpts+=(--light --syntax-theme ${DELTA_LIGHT_THEME:-GitHub})
fi
echo delta "${deltaOpts[@]}" "$@"
exec delta "${deltaOpts[@]}" "$@"
