#compdef git-diff-file-status
#description alias for git-diff --name-status --color

_git-diff-file-status () {
  words=(diff --name-status --color "${words[@]:1}")
  (( CURRENT += 3 ))
  _git-diff
}
